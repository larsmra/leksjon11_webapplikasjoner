import React, { useState } from 'react';
import styled from 'styled-components';
import PollCreator from '../components/PollCreator';

const CreatePoll = () => <PollCreator />;

export default CreatePoll;
