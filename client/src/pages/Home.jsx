import React, { useEffect, useState } from 'react';
import styled from 'styled-components';
// import { create } from '../../../server/controllers/user';
import Poll from '../components/Poll';
import { get, create } from '../utils/pollService';

const Home = () => <section />;

export default Home;
